apply plugin: 'com.android.application'
apply plugin: 'kotlin-android'
apply plugin: 'com.getkeepsafe.dexcount'

android {
    compileSdkVersion project.targetSdkVersion
    buildToolsVersion project.buildToolsVersion

    dexOptions {
        javaMaxHeapSize "3g"
    }

    defaultConfig {
        minSdkVersion project.minSdkVersion
        targetSdkVersion project.targetSdkVersion
        multiDexEnabled true
        versionName project.versionName
        vectorDrawables.useSupportLibrary = true
    }

    sourceSets {
        lightningPlus.setRoot('src/LightningPlus')
        lightningLite.setRoot('src/LightningLite')
    }

    buildTypes {
        debug {
            minifyEnabled false
            shrinkResources false
            proguardFiles 'proguard-project.txt'
        }

        release {
            minifyEnabled true
            shrinkResources true
            proguardFiles 'proguard-project.txt'
        }

        sonar1Debug {
            initWith debug
            buildConfigField 'String', 'ANALYTICS_ID', '"UA-113622467-1"'
        }

        sonar1Release {
            initWith release
            buildConfigField 'String', 'ANALYTICS_ID', '"UA-113622467-1"'
        }

        sonar2Debug {
            initWith debug
            buildConfigField 'String', 'ANALYTICS_ID', '"UA-113672241-1"'
        }

        sonar2Release {
            initWith release
            buildConfigField 'String', 'ANALYTICS_ID', '"UA-113672241-1"'
        }

        sonar3Debug {
            initWith debug
            buildConfigField 'String', 'ANALYTICS_ID', '"UA-113638674-1"'
        }

        sonar3Release {
            initWith release
            buildConfigField 'String', 'ANALYTICS_ID', '"UA-113638674-1"'
        }

        sonar4Debug {
            initWith debug
            buildConfigField 'String', 'ANALYTICS_ID', '"UA-113652365-1"'
        }

        sonar4Release {
            initWith release
            buildConfigField 'String', 'ANALYTICS_ID', '"UA-113652365-1"'
        }
    }

    productFlavors {
        lightningPlus {
            buildConfigField "boolean", "FULL_VERSION", "Boolean.parseBoolean(\"true\")"
            applicationId "acr.browser.lightning"
            versionCode project.versionCode_plus
        }

        lightningLite {
            buildConfigField "boolean", "FULL_VERSION", "Boolean.parseBoolean(\"false\")"
            applicationId "acr.browser.sonar"
            versionCode project.versionCode_lite
        }
    }

    variantFilter { variant ->
        def buildType = variant.buildType.name
        if (!buildType.contains("Debug") && !buildType.contains("Release")) {
            setIgnore(true)
        }
    }

    lintOptions {
        abortOnError true
    }

    packagingOptions {
        exclude '.readme'
        exclude 'META-INF/DEPENDENCIES.txt'
        exclude 'META-INF/LICENSE.txt'
        exclude 'META-INF/NOTICE.txt'
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
        exclude 'META-INF/DEPENDENCIES'
        exclude 'META-INF/notice.txt'
        exclude 'META-INF/license.txt'
        exclude 'META-INF/dependencies.txt'
        exclude 'META-INF/LGPL2.1'
    }

    dexOptions {
        jumboMode true
    }
}

dexcount {
    includeClasses = false
    includeFieldCount = false
    format = "tree"
    orderByMethodCount = true
    verbose = false
}

dependencies {

    compile('com.mopub:mopub-sdk:4.17.0@aar') {
        transitive = true
    }
    compile 'com.google.ads.mediation:mopub:4.17.0.0'

    //Google play services
    compile 'com.google.android.gms:play-services:11.6.0'
    compile 'com.google.android.gms:play-services-ads:11.6.0'
    compile 'com.google.android.gms:play-services-analytics:11.6.0'

    // support libraries
    def supportLibVersion = '25.4.0'
    compile "com.android.support:palette-v7:$supportLibVersion"
    compile "com.android.support:appcompat-v7:$supportLibVersion"
    compile "com.android.support:design:$supportLibVersion"
    compile "com.android.support:recyclerview-v7:$supportLibVersion"
    compile "com.android.support:support-v4:$supportLibVersion"

    // html parsing for reading mode

    // dependency injection
    def daggerVersion = '2.11'
    compile "com.google.dagger:dagger:$daggerVersion"
    annotationProcessor "com.google.dagger:dagger-compiler:$daggerVersion"

    // view binding
    def butterknifeVersion = '8.6.0'
    compile "com.jakewharton:butterknife:$butterknifeVersion"
    annotationProcessor "com.jakewharton:butterknife-compiler:$butterknifeVersion"

    // permissions

    // proxy support

    // tor proxy
    def netcipherVersion = '2.0.0-alpha1'
    compile "info.guardianproject.netcipher:netcipher:$netcipherVersion"
    compile "info.guardianproject.netcipher:netcipher-webkit:$netcipherVersion"

    // memory leak analysis
    def leakCanaryVersion = '1.5.1'
    sonar1DebugCompile "com.squareup.leakcanary:leakcanary-android:$leakCanaryVersion"
    sonar2DebugCompile "com.squareup.leakcanary:leakcanary-android:$leakCanaryVersion"
    sonar3DebugCompile "com.squareup.leakcanary:leakcanary-android:$leakCanaryVersion"
    sonar4DebugCompile "com.squareup.leakcanary:leakcanary-android:$leakCanaryVersion"
    sonar1ReleaseCompile "com.squareup.leakcanary:leakcanary-android-no-op:$leakCanaryVersion"
    sonar2ReleaseCompile "com.squareup.leakcanary:leakcanary-android-no-op:$leakCanaryVersion"
    sonar3ReleaseCompile "com.squareup.leakcanary:leakcanary-android-no-op:$leakCanaryVersion"
    sonar4ReleaseCompile "com.squareup.leakcanary:leakcanary-android-no-op:$leakCanaryVersion"

    // Kotlin
    compile "org.jetbrains.kotlin:kotlin-stdlib-jre7:$kotlin_version"

    //-------------- ADDS DEPENDENCIES / MOPUB

    // For banners
    compile('com.mopub:mopub-sdk-banner:4.16.0@aar') {
        transitive = true
    }

    // For interstitials
    compile('com.mopub:mopub-sdk-interstitial:4.16.0@aar') {
        transitive = true
    }

    // For rewarded videos. This will automatically also include interstitials
    compile('com.mopub:mopub-sdk-rewardedvideo:4.16.0@aar') {
        transitive = true
    }

    // For native static (images).
    compile('com.mopub:mopub-sdk-native-static:4.16.0@aar') {
        transitive = true
    }

    // For native video. This will automatically also include native static
    compile('com.mopub:mopub-sdk-native-video:4.16.0@aar') {
        transitive = true
    }

    compile 'org.jsoup:jsoup:1.10.2'
    compile 'com.anthonycr.grant:permissions:1.1.2'
    compile 'net.i2p.android:client:0.8'
    compile 'com.squareup.okhttp3:okhttp:3.8.0'
    compile 'com.anthonycr.bonsai:bonsai:1.1.0'
    compile 'com.anthonycr.progress:animated-progress:1.0'
    compile 'com.mopub:mopub-sdk:4.16.1'
    compile 'com.android.support.constraint:constraint-layout:1.0.0-alpha8'
    compile 'com.android.support:multidex:1.0.1'
    testCompile 'junit:junit:4.12'
    provided 'javax.annotation:jsr250-api:1.0'
}
